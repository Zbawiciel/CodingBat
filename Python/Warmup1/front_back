# === DESCRIPTION ===

# Given a string, return a new string where the first and last chars have been exchanged.

# front_back('code') → 'eodc'
# front_back('a') → 'a'
# front_back('ab') → 'ba'

# === MY SOLUTION ===

def front_back(str):
  l = len(str)
  if l>1:
    str = str[l-1:l] + str[1:l-1] + str[0:1]
    return str
  else:
    return str
